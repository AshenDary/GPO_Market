# Core
pandas>=2.2
numpy>=1.26
requests>=2.31

# CLI for the evaluator
typer>=0.12

# Dashboard
streamlit>=1.39

# Plotting (EDA notebook)
matplotlib>=3.8
seaborn>=0.13

# Notebooks
jupyter>=1.0

# Testing
pytest>=8.0


# scikit-learn        # once you're regressing structural features against
#                        gpovalues values for low-confidence items (Phase 4)
# statsmodels          # proper time-series trend modeling once you have
#                        many snapshot dates, instead of first-vs-last delta
# apscheduler / cron   # automate the daily snapshot pull instead of running
#                        run_ingest_gpovalues.py by hand
